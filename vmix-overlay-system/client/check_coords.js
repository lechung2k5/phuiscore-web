const fs = require('fs');
const path = require('path');

// A simple script to read PNG pixels and find the bounding box
// PNG format parsing is complex, but we can just use the 'pngjs' library which is commonly installed or we can just use 'jimp' or 'canvas'
console.log("We need to find exact coordinates");
